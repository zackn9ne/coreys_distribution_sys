run gulp watch, to watch on 9000

run gulp to build site to dist/

*deploy*
git push origin master
git subtree push --prefix dist origin gh-pages
ssh into corey server
cd html
rm -rf coreys_distribution_sys
git clone https://github.com/zackn9ne/coreys_distribution_sys.git
exit
